/**
 * Enum que define los entornos disponibles para el servicio RedPay.
 */
export enum RedPayEnvironment {
  /** Entorno de producción: se utiliza para operaciones reales en un ambiente productivo */
  Production = "production",

  /** Entorno de integración: se utiliza para pruebas y desarrollo */
  Integration = "integration",
}
