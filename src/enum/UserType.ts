/**
 * Enum que define los tipos de usuarios en el sistema.
 */
export enum UserType {
  /** Usuario de tipo recaudador (collector) */
  COLLECTOR = "collector",

  /** Usuario de tipo pagador (payer) */
  PAYER = "payer",
}
