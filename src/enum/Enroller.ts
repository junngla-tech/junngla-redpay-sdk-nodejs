export enum Enroller {
  /** Enroller de tipo recaudador (collector) */
  COLLECTOR = "collector",

  /** Enroller de tipo pagador (payer) */
  PAYER = "payer",

  /** Enroller de tipo pagador y recaudador (dual) */
  DUAL = "dual",
}
